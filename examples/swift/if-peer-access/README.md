# if-peer-access

Swift example: move data between the two partitions of a Duo card across the
Infinity Fabric bridge — compare explicit staged copies against kernel-driven
remote access, and print measured timings for both directions.

The measured deltas here should match `tools/if-bench` numbers for the same
machine (and this example doubles as a minimal repro for bug reports).

**Status:** planned (Phase 4).
