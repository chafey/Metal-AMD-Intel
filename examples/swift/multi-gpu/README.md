# multi-gpu

Swift example: drive two `MTLDevice`s from one process — independent command
queues, explicit cross-device synchronization, and a workload split across
both partitions of a Duo card (or two separate cards, if present).

**Status:** planned (Phase 4).
